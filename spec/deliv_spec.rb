require 'spec_helper'

describe Deliv::Sdk do
  it 'has a version number' do
    expect(Deliv::Sdk::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
